# ==============================================================================
# ForgeMD Environment Configuration
# Copy this file to .env and adjust the values
# ==============================================================================

# ------------------------------------------------------------------------------
# Networking
# ------------------------------------------------------------------------------
# The port the API binds to (Default 3000)
PORT=3000
HOST=127.0.0.1

# ------------------------------------------------------------------------------
# DuckDNS Setup
# ------------------------------------------------------------------------------
# Enter your DuckDNS subdomain name (without .duckdns.org)
DUCKDNS_DOMAIN=your_subdomain

# Enter your DuckDNS token
DUCKDNS_TOKEN=your-token-here-xxxx-xxxx

# ------------------------------------------------------------------------------
# Security
# ------------------------------------------------------------------------------
# The node environment (should be production when deployed)
NODE_ENV=production

# ------------------------------------------------------------------------------
# Storage Paths (Overrides)
# ------------------------------------------------------------------------------
# If deploying using setup.sh, these are set automatically by systemd:
# STORAGE_UPLOADS=/srv/forgemd/uploads
# STORAGE_OUTPUTS=/srv/forgemd/outputs
# STORAGE_TMP=/srv/forgemd/tmp
# STORAGE_DATA=/srv/forgemd/data

# ------------------------------------------------------------------------------
# External Services
# ------------------------------------------------------------------------------
# Ollama endpoint
# OLLAMA_URL=http://127.0.0.1:11434
